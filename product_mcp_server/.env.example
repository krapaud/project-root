PRODUCT_API_BASE_URL=http://localhost:5001
# For manual (non-Docker) runs, point at the Backoffice's local SQLite file:
STOCK_DATABASE_URL=sqlite:///../backoffice/backoffice.db
# Docker Compose overrides this to Postgres (see ../docker-compose.yml).
